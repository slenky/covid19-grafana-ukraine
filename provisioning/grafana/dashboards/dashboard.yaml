apiVersion: 1

providers:
- name: 'COVID-19 Ukraine'
  orgId: 1
  folder: ''
  type: file
  disableDeletion: false
  editable: true
  # <int> how often Grafana will scan for changed dashboards
  updateIntervalSeconds: 10
  # <bool> allow updating provisioned dashboards from the UI
  allowUiUpdates: true
  options:
    path: /etc/grafana/provisioning/dashboards/covid-ukraine.json
